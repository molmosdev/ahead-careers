import { FadeInOnEnterViewDirective } from './fade-in-on-enter-view.directive';

describe('FadeInOnEnterViewDirective', () => {
  it('should create an instance', () => {
    const directive = new FadeInOnEnterViewDirective();
    expect(directive).toBeTruthy();
  });
});
